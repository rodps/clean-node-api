version: '3.1'

services:
  db:
    image: postgres:13
    container_name: integration-tests-prisma
    restart: always
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: prisma
      POSTGRES_PASSWORD: prisma
      POSTGRES_DB: tests